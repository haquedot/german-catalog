/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      locales: ['de'], // Only German
      defaultLocale: 'de', // Default and only locale is German
    },
  };
  
  export default nextConfig;
  